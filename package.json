{
  "name": "coreweb-public",
  "private": true,
  "packageManager": "npm@11.12.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test:rules": "npx -y firebase-tools@latest emulators:exec --only firestore \"node --test tests/rules/firestore.spec.js\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase-admin": "^14.1.0",
    "turbo": "latest"
  }
}
